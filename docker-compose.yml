version: '3.8'

services:
  bot:
    build: .
    container_name: yt-vantage-bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
      - DB_PATH=/data/bot_data.db
    volumes:
      - bot_data:/data
    restart: unless-stopped

volumes:
  bot_data:
